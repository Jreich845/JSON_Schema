{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "company_name": {
      "type": "string",
      "description": "The name of the company providing the product or service"
    },
    "customer_name": {
      "type": "string",
      "description": "The name of the customer or client"
    },
    "product_name": {
      "type": "string",
      "description": "The name of the product being discussed or ordered"
    },
    "pack_size": {
      "type": "number",
      "description": "The numerical value of the packaging size (e.g., 25, 1)"
    },
    "pack_size_unit": {
      "type": "string",
      "description": "The unit for the pack size (e.g., Lbs, Kgs)"
    },
    "lead_time": {
      "type": "string",
      "description": "The expected lead time or delivery time frame for the product"
    },
    "shipping_option": {
      "type": "string",
      "description": "The selected or available shipping option (e.g., air freight, ground, express)"
    },
    "price": {
      "type": "number",
      "description": "The price of the product (e.g., 250, 100.5)"
    },
    "price_unit": {
      "type": "string",
      "description": "The unit for the price (e.g., $/kg, $/lb)"
    },
    "quantity": {
      "type": "number",
      "description": "The number of units being ordered"
    },
    "quantity_unit": {
      "type": "string",
      "description": "The unit for the quantity (e.g., drum, bottle)"
    }
  },
  "required": [
    "company_name",
    "customer_name",
    "product_name",
    "pack_size",
    "pack_size_unit",
    "lead_time",
    "shipping_option",
    "price",
    "price_unit",
    "quantity",
    "quantity_unit"
  ]
}
